///////////////////////////////////////////
//
// RISC-V Architectural Functional Coverage Covergroups
//
// This file is autogenerated by covergroupgen.
// Do not modify directly.
//
// Copyright (C) 2024 Harvey Mudd College, 10x Engineers, UET Lahore, Habib University
//
// SPDX-License-Identifier: Apache-2.0
//
////////////////////////////////////////////////////////////////////////////////////////////////

`define COVER_MISALIGNV
covergroup MisalignV_vl1re16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl1re32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl1re64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl1re8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl2re16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl2re32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl2re64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl2re8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl4re16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl4re32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl4re64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl4re8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl8re16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl8re32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl8re64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vl8re8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vle16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vle16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vle32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vle32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vle64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vle64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vle8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vle8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlm_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vloxei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vloxei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg2ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg2ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vloxseg2ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vloxseg2ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg3ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg3ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vloxseg3ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vloxseg3ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg4ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg4ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vloxseg4ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vloxseg4ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg5ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg5ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vloxseg5ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vloxseg5ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg6ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg6ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vloxseg6ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vloxseg6ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg7ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg7ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vloxseg7ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vloxseg7ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg8ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vloxseg8ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vloxseg8ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vloxseg8ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlse16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlse32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlse64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlse8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg2e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg2e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg2e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg2e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg2e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg2e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg2e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg2e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg3e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg3e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg3e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg3e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg3e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg3e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg3e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg3e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg4e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg4e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg4e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg4e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg4e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg4e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg4e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg4e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg5e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg5e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg5e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg5e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg5e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg5e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg5e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg5e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg6e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg6e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg6e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg6e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg6e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg6e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg6e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg6e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg7e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg7e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg7e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg7e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg7e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg7e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg7e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg7e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg8e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg8e16ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg8e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg8e32ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg8e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg8e64ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg8e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlseg8e8ff_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg2e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg2e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg2e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg2e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg3e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg3e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg3e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg3e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg4e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg4e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg4e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg4e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg5e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg5e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg5e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg5e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg6e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg6e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg6e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg6e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg7e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg7e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg7e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg7e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg8e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg8e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg8e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vlsseg8e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vluxei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vluxei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg2ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg2ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vluxseg2ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vluxseg2ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg3ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg3ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vluxseg3ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vluxseg3ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg4ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg4ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vluxseg4ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vluxseg4ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg5ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg5ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vluxseg5ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vluxseg5ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg6ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg6ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vluxseg6ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vluxseg6ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg7ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg7ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vluxseg7ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vluxseg7ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg8ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vluxseg8ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vluxseg8ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vluxseg8ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vs1r_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vs2r_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vs4r_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vs8r_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_wholereg_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise whole-register vector load/store (vl<nf>r / vs<nf>r) instructions
// whose base address is not naturally aligned to max(EEW/8, SEWMIN/8).
// Implementations are permitted to raise a misaligned-address exception on
// such accesses or to complete them successfully; both outcomes are valid.
// This coverpoint samples occurrence of the misaligned base with a valid
// vtype and is intended to be applied to the whole-register L/S instruction
// list.


    vtype_valid_0293c6: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // any supported EEW of 2 bytes or larger.
    base_misalign_0293c6 : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_wholereg_misaligned : cross vtype_valid_0293c6, base_misalign_0293c6;

//// end cp_misalignv_ls_wholereg_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vse16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vse32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vse64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vse8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsm_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsoxei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsoxei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg2ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg2ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsoxseg2ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsoxseg2ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg3ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg3ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsoxseg3ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsoxseg3ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg4ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg4ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsoxseg4ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsoxseg4ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg5ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg5ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsoxseg5ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsoxseg5ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg6ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg6ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsoxseg6ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsoxseg6ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg7ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg7ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsoxseg7ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsoxseg7ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg8ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsoxseg8ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsoxseg8ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsoxseg8ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsse16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsse32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsse64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsse8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg2e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg2e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg2e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg2e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg3e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg3e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg3e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg3e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg4e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg4e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg4e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg4e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg5e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg5e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg5e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg5e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg6e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg6e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg6e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg6e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg7e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg7e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg7e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg7e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg8e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg8e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg8e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsseg8e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg2e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg2e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg2e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg2e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg3e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg3e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg3e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg3e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg4e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg4e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg4e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg4e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg5e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg5e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg5e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg5e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg6e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg6e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg6e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg6e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg7e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg7e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg7e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg7e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg8e16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg8e32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg8e64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vssseg8e8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsuxei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsuxei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg2ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg2ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsuxseg2ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsuxseg2ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg3ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg3ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsuxseg3ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsuxseg3ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg4ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg4ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsuxseg4ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsuxseg4ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg5ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg5ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsuxseg5ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsuxseg5ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg6ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg6ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsuxseg6ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsuxseg6ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg7ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg7ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsuxseg7ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsuxseg7ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg8ei16_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
covergroup MisalignV_vsuxseg8ei32_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`ifdef XLEN64
covergroup MisalignV_vsuxseg8ei64_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
`endif
covergroup MisalignV_vsuxseg8ei8_v_cg with function sample(ins_t ins);
    option.per_instance = 0;
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
// cp_misalignv_ls_element_misaligned
// //////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Exercise vector memory accesses whose base address is not naturally aligned
// to the element size. The rule permits either a successful transfer or an
// address-misaligned exception on the offending element; both outcomes are
// architecturally valid, so this coverpoint samples only the occurrence of a
// misaligned base with a valid vtype.


    vtype_valid_42737e: coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vtype", "vill") {
        bins valid = {1'b0};
    }

    // Non-zero low bits of the base address indicate misalignment relative to
    // elements of width >= 2 bytes. All seven non-aligned offsets are covered.
    base_misalign_42737e : coverpoint ins.current.rs1_val[2:0] {
        bins offsets[] = {[3'b001:3'b111]};
    }

    cp_misalignv_ls_element_misaligned : cross vtype_valid_42737e, base_misalign_42737e;

//// end cp_misalignv_ls_element_misaligned ///////////////////////////////////////////////////////////////////

endgroup
// ---------------------
function void misalignv_sample(int hart, int issue, ins_t ins);

    case (traceDataQ[hart][issue][0].inst_id)
        INSTR_VL1RE16_V     : begin
            MisalignV_vl1re16_v_cg.sample(ins);
        end
        INSTR_VL1RE32_V     : begin
            MisalignV_vl1re32_v_cg.sample(ins);
        end
        INSTR_VL1RE64_V     : begin
            MisalignV_vl1re64_v_cg.sample(ins);
        end
        INSTR_VL1RE8_V     : begin
            MisalignV_vl1re8_v_cg.sample(ins);
        end
        INSTR_VL2RE16_V     : begin
            MisalignV_vl2re16_v_cg.sample(ins);
        end
        INSTR_VL2RE32_V     : begin
            MisalignV_vl2re32_v_cg.sample(ins);
        end
        INSTR_VL2RE64_V     : begin
            MisalignV_vl2re64_v_cg.sample(ins);
        end
        INSTR_VL2RE8_V     : begin
            MisalignV_vl2re8_v_cg.sample(ins);
        end
        INSTR_VL4RE16_V     : begin
            MisalignV_vl4re16_v_cg.sample(ins);
        end
        INSTR_VL4RE32_V     : begin
            MisalignV_vl4re32_v_cg.sample(ins);
        end
        INSTR_VL4RE64_V     : begin
            MisalignV_vl4re64_v_cg.sample(ins);
        end
        INSTR_VL4RE8_V     : begin
            MisalignV_vl4re8_v_cg.sample(ins);
        end
        INSTR_VL8RE16_V     : begin
            MisalignV_vl8re16_v_cg.sample(ins);
        end
        INSTR_VL8RE32_V     : begin
            MisalignV_vl8re32_v_cg.sample(ins);
        end
        INSTR_VL8RE64_V     : begin
            MisalignV_vl8re64_v_cg.sample(ins);
        end
        INSTR_VL8RE8_V     : begin
            MisalignV_vl8re8_v_cg.sample(ins);
        end
        INSTR_VLE16_V     : begin
            MisalignV_vle16_v_cg.sample(ins);
        end
        INSTR_VLE16FF_V     : begin
            MisalignV_vle16ff_v_cg.sample(ins);
        end
        INSTR_VLE32_V     : begin
            MisalignV_vle32_v_cg.sample(ins);
        end
        INSTR_VLE32FF_V     : begin
            MisalignV_vle32ff_v_cg.sample(ins);
        end
        INSTR_VLE64_V     : begin
            MisalignV_vle64_v_cg.sample(ins);
        end
        INSTR_VLE64FF_V     : begin
            MisalignV_vle64ff_v_cg.sample(ins);
        end
        INSTR_VLE8_V     : begin
            MisalignV_vle8_v_cg.sample(ins);
        end
        INSTR_VLE8FF_V     : begin
            MisalignV_vle8ff_v_cg.sample(ins);
        end
        INSTR_VLM_V     : begin
            MisalignV_vlm_v_cg.sample(ins);
        end
        INSTR_VLOXEI16_V     : begin
            MisalignV_vloxei16_v_cg.sample(ins);
        end
        INSTR_VLOXEI32_V     : begin
            MisalignV_vloxei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLOXEI64_V     : begin
            MisalignV_vloxei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLOXEI8_V     : begin
            MisalignV_vloxei8_v_cg.sample(ins);
        end
        INSTR_VLOXSEG2EI16_V     : begin
            MisalignV_vloxseg2ei16_v_cg.sample(ins);
        end
        INSTR_VLOXSEG2EI32_V     : begin
            MisalignV_vloxseg2ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLOXSEG2EI64_V     : begin
            MisalignV_vloxseg2ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLOXSEG2EI8_V     : begin
            MisalignV_vloxseg2ei8_v_cg.sample(ins);
        end
        INSTR_VLOXSEG3EI16_V     : begin
            MisalignV_vloxseg3ei16_v_cg.sample(ins);
        end
        INSTR_VLOXSEG3EI32_V     : begin
            MisalignV_vloxseg3ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLOXSEG3EI64_V     : begin
            MisalignV_vloxseg3ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLOXSEG3EI8_V     : begin
            MisalignV_vloxseg3ei8_v_cg.sample(ins);
        end
        INSTR_VLOXSEG4EI16_V     : begin
            MisalignV_vloxseg4ei16_v_cg.sample(ins);
        end
        INSTR_VLOXSEG4EI32_V     : begin
            MisalignV_vloxseg4ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLOXSEG4EI64_V     : begin
            MisalignV_vloxseg4ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLOXSEG4EI8_V     : begin
            MisalignV_vloxseg4ei8_v_cg.sample(ins);
        end
        INSTR_VLOXSEG5EI16_V     : begin
            MisalignV_vloxseg5ei16_v_cg.sample(ins);
        end
        INSTR_VLOXSEG5EI32_V     : begin
            MisalignV_vloxseg5ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLOXSEG5EI64_V     : begin
            MisalignV_vloxseg5ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLOXSEG5EI8_V     : begin
            MisalignV_vloxseg5ei8_v_cg.sample(ins);
        end
        INSTR_VLOXSEG6EI16_V     : begin
            MisalignV_vloxseg6ei16_v_cg.sample(ins);
        end
        INSTR_VLOXSEG6EI32_V     : begin
            MisalignV_vloxseg6ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLOXSEG6EI64_V     : begin
            MisalignV_vloxseg6ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLOXSEG6EI8_V     : begin
            MisalignV_vloxseg6ei8_v_cg.sample(ins);
        end
        INSTR_VLOXSEG7EI16_V     : begin
            MisalignV_vloxseg7ei16_v_cg.sample(ins);
        end
        INSTR_VLOXSEG7EI32_V     : begin
            MisalignV_vloxseg7ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLOXSEG7EI64_V     : begin
            MisalignV_vloxseg7ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLOXSEG7EI8_V     : begin
            MisalignV_vloxseg7ei8_v_cg.sample(ins);
        end
        INSTR_VLOXSEG8EI16_V     : begin
            MisalignV_vloxseg8ei16_v_cg.sample(ins);
        end
        INSTR_VLOXSEG8EI32_V     : begin
            MisalignV_vloxseg8ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLOXSEG8EI64_V     : begin
            MisalignV_vloxseg8ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLOXSEG8EI8_V     : begin
            MisalignV_vloxseg8ei8_v_cg.sample(ins);
        end
        INSTR_VLSE16_V     : begin
            MisalignV_vlse16_v_cg.sample(ins);
        end
        INSTR_VLSE32_V     : begin
            MisalignV_vlse32_v_cg.sample(ins);
        end
        INSTR_VLSE64_V     : begin
            MisalignV_vlse64_v_cg.sample(ins);
        end
        INSTR_VLSE8_V     : begin
            MisalignV_vlse8_v_cg.sample(ins);
        end
        INSTR_VLSEG2E16_V     : begin
            MisalignV_vlseg2e16_v_cg.sample(ins);
        end
        INSTR_VLSEG2E16FF_V     : begin
            MisalignV_vlseg2e16ff_v_cg.sample(ins);
        end
        INSTR_VLSEG2E32_V     : begin
            MisalignV_vlseg2e32_v_cg.sample(ins);
        end
        INSTR_VLSEG2E32FF_V     : begin
            MisalignV_vlseg2e32ff_v_cg.sample(ins);
        end
        INSTR_VLSEG2E64_V     : begin
            MisalignV_vlseg2e64_v_cg.sample(ins);
        end
        INSTR_VLSEG2E64FF_V     : begin
            MisalignV_vlseg2e64ff_v_cg.sample(ins);
        end
        INSTR_VLSEG2E8_V     : begin
            MisalignV_vlseg2e8_v_cg.sample(ins);
        end
        INSTR_VLSEG2E8FF_V     : begin
            MisalignV_vlseg2e8ff_v_cg.sample(ins);
        end
        INSTR_VLSEG3E16_V     : begin
            MisalignV_vlseg3e16_v_cg.sample(ins);
        end
        INSTR_VLSEG3E16FF_V     : begin
            MisalignV_vlseg3e16ff_v_cg.sample(ins);
        end
        INSTR_VLSEG3E32_V     : begin
            MisalignV_vlseg3e32_v_cg.sample(ins);
        end
        INSTR_VLSEG3E32FF_V     : begin
            MisalignV_vlseg3e32ff_v_cg.sample(ins);
        end
        INSTR_VLSEG3E64_V     : begin
            MisalignV_vlseg3e64_v_cg.sample(ins);
        end
        INSTR_VLSEG3E64FF_V     : begin
            MisalignV_vlseg3e64ff_v_cg.sample(ins);
        end
        INSTR_VLSEG3E8_V     : begin
            MisalignV_vlseg3e8_v_cg.sample(ins);
        end
        INSTR_VLSEG3E8FF_V     : begin
            MisalignV_vlseg3e8ff_v_cg.sample(ins);
        end
        INSTR_VLSEG4E16_V     : begin
            MisalignV_vlseg4e16_v_cg.sample(ins);
        end
        INSTR_VLSEG4E16FF_V     : begin
            MisalignV_vlseg4e16ff_v_cg.sample(ins);
        end
        INSTR_VLSEG4E32_V     : begin
            MisalignV_vlseg4e32_v_cg.sample(ins);
        end
        INSTR_VLSEG4E32FF_V     : begin
            MisalignV_vlseg4e32ff_v_cg.sample(ins);
        end
        INSTR_VLSEG4E64_V     : begin
            MisalignV_vlseg4e64_v_cg.sample(ins);
        end
        INSTR_VLSEG4E64FF_V     : begin
            MisalignV_vlseg4e64ff_v_cg.sample(ins);
        end
        INSTR_VLSEG4E8_V     : begin
            MisalignV_vlseg4e8_v_cg.sample(ins);
        end
        INSTR_VLSEG4E8FF_V     : begin
            MisalignV_vlseg4e8ff_v_cg.sample(ins);
        end
        INSTR_VLSEG5E16_V     : begin
            MisalignV_vlseg5e16_v_cg.sample(ins);
        end
        INSTR_VLSEG5E16FF_V     : begin
            MisalignV_vlseg5e16ff_v_cg.sample(ins);
        end
        INSTR_VLSEG5E32_V     : begin
            MisalignV_vlseg5e32_v_cg.sample(ins);
        end
        INSTR_VLSEG5E32FF_V     : begin
            MisalignV_vlseg5e32ff_v_cg.sample(ins);
        end
        INSTR_VLSEG5E64_V     : begin
            MisalignV_vlseg5e64_v_cg.sample(ins);
        end
        INSTR_VLSEG5E64FF_V     : begin
            MisalignV_vlseg5e64ff_v_cg.sample(ins);
        end
        INSTR_VLSEG5E8_V     : begin
            MisalignV_vlseg5e8_v_cg.sample(ins);
        end
        INSTR_VLSEG5E8FF_V     : begin
            MisalignV_vlseg5e8ff_v_cg.sample(ins);
        end
        INSTR_VLSEG6E16_V     : begin
            MisalignV_vlseg6e16_v_cg.sample(ins);
        end
        INSTR_VLSEG6E16FF_V     : begin
            MisalignV_vlseg6e16ff_v_cg.sample(ins);
        end
        INSTR_VLSEG6E32_V     : begin
            MisalignV_vlseg6e32_v_cg.sample(ins);
        end
        INSTR_VLSEG6E32FF_V     : begin
            MisalignV_vlseg6e32ff_v_cg.sample(ins);
        end
        INSTR_VLSEG6E64_V     : begin
            MisalignV_vlseg6e64_v_cg.sample(ins);
        end
        INSTR_VLSEG6E64FF_V     : begin
            MisalignV_vlseg6e64ff_v_cg.sample(ins);
        end
        INSTR_VLSEG6E8_V     : begin
            MisalignV_vlseg6e8_v_cg.sample(ins);
        end
        INSTR_VLSEG6E8FF_V     : begin
            MisalignV_vlseg6e8ff_v_cg.sample(ins);
        end
        INSTR_VLSEG7E16_V     : begin
            MisalignV_vlseg7e16_v_cg.sample(ins);
        end
        INSTR_VLSEG7E16FF_V     : begin
            MisalignV_vlseg7e16ff_v_cg.sample(ins);
        end
        INSTR_VLSEG7E32_V     : begin
            MisalignV_vlseg7e32_v_cg.sample(ins);
        end
        INSTR_VLSEG7E32FF_V     : begin
            MisalignV_vlseg7e32ff_v_cg.sample(ins);
        end
        INSTR_VLSEG7E64_V     : begin
            MisalignV_vlseg7e64_v_cg.sample(ins);
        end
        INSTR_VLSEG7E64FF_V     : begin
            MisalignV_vlseg7e64ff_v_cg.sample(ins);
        end
        INSTR_VLSEG7E8_V     : begin
            MisalignV_vlseg7e8_v_cg.sample(ins);
        end
        INSTR_VLSEG7E8FF_V     : begin
            MisalignV_vlseg7e8ff_v_cg.sample(ins);
        end
        INSTR_VLSEG8E16_V     : begin
            MisalignV_vlseg8e16_v_cg.sample(ins);
        end
        INSTR_VLSEG8E16FF_V     : begin
            MisalignV_vlseg8e16ff_v_cg.sample(ins);
        end
        INSTR_VLSEG8E32_V     : begin
            MisalignV_vlseg8e32_v_cg.sample(ins);
        end
        INSTR_VLSEG8E32FF_V     : begin
            MisalignV_vlseg8e32ff_v_cg.sample(ins);
        end
        INSTR_VLSEG8E64_V     : begin
            MisalignV_vlseg8e64_v_cg.sample(ins);
        end
        INSTR_VLSEG8E64FF_V     : begin
            MisalignV_vlseg8e64ff_v_cg.sample(ins);
        end
        INSTR_VLSEG8E8_V     : begin
            MisalignV_vlseg8e8_v_cg.sample(ins);
        end
        INSTR_VLSEG8E8FF_V     : begin
            MisalignV_vlseg8e8ff_v_cg.sample(ins);
        end
        INSTR_VLSSEG2E16_V     : begin
            MisalignV_vlsseg2e16_v_cg.sample(ins);
        end
        INSTR_VLSSEG2E32_V     : begin
            MisalignV_vlsseg2e32_v_cg.sample(ins);
        end
        INSTR_VLSSEG2E64_V     : begin
            MisalignV_vlsseg2e64_v_cg.sample(ins);
        end
        INSTR_VLSSEG2E8_V     : begin
            MisalignV_vlsseg2e8_v_cg.sample(ins);
        end
        INSTR_VLSSEG3E16_V     : begin
            MisalignV_vlsseg3e16_v_cg.sample(ins);
        end
        INSTR_VLSSEG3E32_V     : begin
            MisalignV_vlsseg3e32_v_cg.sample(ins);
        end
        INSTR_VLSSEG3E64_V     : begin
            MisalignV_vlsseg3e64_v_cg.sample(ins);
        end
        INSTR_VLSSEG3E8_V     : begin
            MisalignV_vlsseg3e8_v_cg.sample(ins);
        end
        INSTR_VLSSEG4E16_V     : begin
            MisalignV_vlsseg4e16_v_cg.sample(ins);
        end
        INSTR_VLSSEG4E32_V     : begin
            MisalignV_vlsseg4e32_v_cg.sample(ins);
        end
        INSTR_VLSSEG4E64_V     : begin
            MisalignV_vlsseg4e64_v_cg.sample(ins);
        end
        INSTR_VLSSEG4E8_V     : begin
            MisalignV_vlsseg4e8_v_cg.sample(ins);
        end
        INSTR_VLSSEG5E16_V     : begin
            MisalignV_vlsseg5e16_v_cg.sample(ins);
        end
        INSTR_VLSSEG5E32_V     : begin
            MisalignV_vlsseg5e32_v_cg.sample(ins);
        end
        INSTR_VLSSEG5E64_V     : begin
            MisalignV_vlsseg5e64_v_cg.sample(ins);
        end
        INSTR_VLSSEG5E8_V     : begin
            MisalignV_vlsseg5e8_v_cg.sample(ins);
        end
        INSTR_VLSSEG6E16_V     : begin
            MisalignV_vlsseg6e16_v_cg.sample(ins);
        end
        INSTR_VLSSEG6E32_V     : begin
            MisalignV_vlsseg6e32_v_cg.sample(ins);
        end
        INSTR_VLSSEG6E64_V     : begin
            MisalignV_vlsseg6e64_v_cg.sample(ins);
        end
        INSTR_VLSSEG6E8_V     : begin
            MisalignV_vlsseg6e8_v_cg.sample(ins);
        end
        INSTR_VLSSEG7E16_V     : begin
            MisalignV_vlsseg7e16_v_cg.sample(ins);
        end
        INSTR_VLSSEG7E32_V     : begin
            MisalignV_vlsseg7e32_v_cg.sample(ins);
        end
        INSTR_VLSSEG7E64_V     : begin
            MisalignV_vlsseg7e64_v_cg.sample(ins);
        end
        INSTR_VLSSEG7E8_V     : begin
            MisalignV_vlsseg7e8_v_cg.sample(ins);
        end
        INSTR_VLSSEG8E16_V     : begin
            MisalignV_vlsseg8e16_v_cg.sample(ins);
        end
        INSTR_VLSSEG8E32_V     : begin
            MisalignV_vlsseg8e32_v_cg.sample(ins);
        end
        INSTR_VLSSEG8E64_V     : begin
            MisalignV_vlsseg8e64_v_cg.sample(ins);
        end
        INSTR_VLSSEG8E8_V     : begin
            MisalignV_vlsseg8e8_v_cg.sample(ins);
        end
        INSTR_VLUXEI16_V     : begin
            MisalignV_vluxei16_v_cg.sample(ins);
        end
        INSTR_VLUXEI32_V     : begin
            MisalignV_vluxei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLUXEI64_V     : begin
            MisalignV_vluxei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLUXEI8_V     : begin
            MisalignV_vluxei8_v_cg.sample(ins);
        end
        INSTR_VLUXSEG2EI16_V     : begin
            MisalignV_vluxseg2ei16_v_cg.sample(ins);
        end
        INSTR_VLUXSEG2EI32_V     : begin
            MisalignV_vluxseg2ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLUXSEG2EI64_V     : begin
            MisalignV_vluxseg2ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLUXSEG2EI8_V     : begin
            MisalignV_vluxseg2ei8_v_cg.sample(ins);
        end
        INSTR_VLUXSEG3EI16_V     : begin
            MisalignV_vluxseg3ei16_v_cg.sample(ins);
        end
        INSTR_VLUXSEG3EI32_V     : begin
            MisalignV_vluxseg3ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLUXSEG3EI64_V     : begin
            MisalignV_vluxseg3ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLUXSEG3EI8_V     : begin
            MisalignV_vluxseg3ei8_v_cg.sample(ins);
        end
        INSTR_VLUXSEG4EI16_V     : begin
            MisalignV_vluxseg4ei16_v_cg.sample(ins);
        end
        INSTR_VLUXSEG4EI32_V     : begin
            MisalignV_vluxseg4ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLUXSEG4EI64_V     : begin
            MisalignV_vluxseg4ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLUXSEG4EI8_V     : begin
            MisalignV_vluxseg4ei8_v_cg.sample(ins);
        end
        INSTR_VLUXSEG5EI16_V     : begin
            MisalignV_vluxseg5ei16_v_cg.sample(ins);
        end
        INSTR_VLUXSEG5EI32_V     : begin
            MisalignV_vluxseg5ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLUXSEG5EI64_V     : begin
            MisalignV_vluxseg5ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLUXSEG5EI8_V     : begin
            MisalignV_vluxseg5ei8_v_cg.sample(ins);
        end
        INSTR_VLUXSEG6EI16_V     : begin
            MisalignV_vluxseg6ei16_v_cg.sample(ins);
        end
        INSTR_VLUXSEG6EI32_V     : begin
            MisalignV_vluxseg6ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLUXSEG6EI64_V     : begin
            MisalignV_vluxseg6ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLUXSEG6EI8_V     : begin
            MisalignV_vluxseg6ei8_v_cg.sample(ins);
        end
        INSTR_VLUXSEG7EI16_V     : begin
            MisalignV_vluxseg7ei16_v_cg.sample(ins);
        end
        INSTR_VLUXSEG7EI32_V     : begin
            MisalignV_vluxseg7ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLUXSEG7EI64_V     : begin
            MisalignV_vluxseg7ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLUXSEG7EI8_V     : begin
            MisalignV_vluxseg7ei8_v_cg.sample(ins);
        end
        INSTR_VLUXSEG8EI16_V     : begin
            MisalignV_vluxseg8ei16_v_cg.sample(ins);
        end
        INSTR_VLUXSEG8EI32_V     : begin
            MisalignV_vluxseg8ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VLUXSEG8EI64_V     : begin
            MisalignV_vluxseg8ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VLUXSEG8EI8_V     : begin
            MisalignV_vluxseg8ei8_v_cg.sample(ins);
        end
        INSTR_VS1R_V     : begin
            MisalignV_vs1r_v_cg.sample(ins);
        end
        INSTR_VS2R_V     : begin
            MisalignV_vs2r_v_cg.sample(ins);
        end
        INSTR_VS4R_V     : begin
            MisalignV_vs4r_v_cg.sample(ins);
        end
        INSTR_VS8R_V     : begin
            MisalignV_vs8r_v_cg.sample(ins);
        end
        INSTR_VSE16_V     : begin
            MisalignV_vse16_v_cg.sample(ins);
        end
        INSTR_VSE32_V     : begin
            MisalignV_vse32_v_cg.sample(ins);
        end
        INSTR_VSE64_V     : begin
            MisalignV_vse64_v_cg.sample(ins);
        end
        INSTR_VSE8_V     : begin
            MisalignV_vse8_v_cg.sample(ins);
        end
        INSTR_VSM_V     : begin
            MisalignV_vsm_v_cg.sample(ins);
        end
        INSTR_VSOXEI16_V     : begin
            MisalignV_vsoxei16_v_cg.sample(ins);
        end
        INSTR_VSOXEI32_V     : begin
            MisalignV_vsoxei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSOXEI64_V     : begin
            MisalignV_vsoxei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSOXEI8_V     : begin
            MisalignV_vsoxei8_v_cg.sample(ins);
        end
        INSTR_VSOXSEG2EI16_V     : begin
            MisalignV_vsoxseg2ei16_v_cg.sample(ins);
        end
        INSTR_VSOXSEG2EI32_V     : begin
            MisalignV_vsoxseg2ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSOXSEG2EI64_V     : begin
            MisalignV_vsoxseg2ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSOXSEG2EI8_V     : begin
            MisalignV_vsoxseg2ei8_v_cg.sample(ins);
        end
        INSTR_VSOXSEG3EI16_V     : begin
            MisalignV_vsoxseg3ei16_v_cg.sample(ins);
        end
        INSTR_VSOXSEG3EI32_V     : begin
            MisalignV_vsoxseg3ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSOXSEG3EI64_V     : begin
            MisalignV_vsoxseg3ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSOXSEG3EI8_V     : begin
            MisalignV_vsoxseg3ei8_v_cg.sample(ins);
        end
        INSTR_VSOXSEG4EI16_V     : begin
            MisalignV_vsoxseg4ei16_v_cg.sample(ins);
        end
        INSTR_VSOXSEG4EI32_V     : begin
            MisalignV_vsoxseg4ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSOXSEG4EI64_V     : begin
            MisalignV_vsoxseg4ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSOXSEG4EI8_V     : begin
            MisalignV_vsoxseg4ei8_v_cg.sample(ins);
        end
        INSTR_VSOXSEG5EI16_V     : begin
            MisalignV_vsoxseg5ei16_v_cg.sample(ins);
        end
        INSTR_VSOXSEG5EI32_V     : begin
            MisalignV_vsoxseg5ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSOXSEG5EI64_V     : begin
            MisalignV_vsoxseg5ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSOXSEG5EI8_V     : begin
            MisalignV_vsoxseg5ei8_v_cg.sample(ins);
        end
        INSTR_VSOXSEG6EI16_V     : begin
            MisalignV_vsoxseg6ei16_v_cg.sample(ins);
        end
        INSTR_VSOXSEG6EI32_V     : begin
            MisalignV_vsoxseg6ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSOXSEG6EI64_V     : begin
            MisalignV_vsoxseg6ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSOXSEG6EI8_V     : begin
            MisalignV_vsoxseg6ei8_v_cg.sample(ins);
        end
        INSTR_VSOXSEG7EI16_V     : begin
            MisalignV_vsoxseg7ei16_v_cg.sample(ins);
        end
        INSTR_VSOXSEG7EI32_V     : begin
            MisalignV_vsoxseg7ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSOXSEG7EI64_V     : begin
            MisalignV_vsoxseg7ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSOXSEG7EI8_V     : begin
            MisalignV_vsoxseg7ei8_v_cg.sample(ins);
        end
        INSTR_VSOXSEG8EI16_V     : begin
            MisalignV_vsoxseg8ei16_v_cg.sample(ins);
        end
        INSTR_VSOXSEG8EI32_V     : begin
            MisalignV_vsoxseg8ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSOXSEG8EI64_V     : begin
            MisalignV_vsoxseg8ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSOXSEG8EI8_V     : begin
            MisalignV_vsoxseg8ei8_v_cg.sample(ins);
        end
        INSTR_VSSE16_V     : begin
            MisalignV_vsse16_v_cg.sample(ins);
        end
        INSTR_VSSE32_V     : begin
            MisalignV_vsse32_v_cg.sample(ins);
        end
        INSTR_VSSE64_V     : begin
            MisalignV_vsse64_v_cg.sample(ins);
        end
        INSTR_VSSE8_V     : begin
            MisalignV_vsse8_v_cg.sample(ins);
        end
        INSTR_VSSEG2E16_V     : begin
            MisalignV_vsseg2e16_v_cg.sample(ins);
        end
        INSTR_VSSEG2E32_V     : begin
            MisalignV_vsseg2e32_v_cg.sample(ins);
        end
        INSTR_VSSEG2E64_V     : begin
            MisalignV_vsseg2e64_v_cg.sample(ins);
        end
        INSTR_VSSEG2E8_V     : begin
            MisalignV_vsseg2e8_v_cg.sample(ins);
        end
        INSTR_VSSEG3E16_V     : begin
            MisalignV_vsseg3e16_v_cg.sample(ins);
        end
        INSTR_VSSEG3E32_V     : begin
            MisalignV_vsseg3e32_v_cg.sample(ins);
        end
        INSTR_VSSEG3E64_V     : begin
            MisalignV_vsseg3e64_v_cg.sample(ins);
        end
        INSTR_VSSEG3E8_V     : begin
            MisalignV_vsseg3e8_v_cg.sample(ins);
        end
        INSTR_VSSEG4E16_V     : begin
            MisalignV_vsseg4e16_v_cg.sample(ins);
        end
        INSTR_VSSEG4E32_V     : begin
            MisalignV_vsseg4e32_v_cg.sample(ins);
        end
        INSTR_VSSEG4E64_V     : begin
            MisalignV_vsseg4e64_v_cg.sample(ins);
        end
        INSTR_VSSEG4E8_V     : begin
            MisalignV_vsseg4e8_v_cg.sample(ins);
        end
        INSTR_VSSEG5E16_V     : begin
            MisalignV_vsseg5e16_v_cg.sample(ins);
        end
        INSTR_VSSEG5E32_V     : begin
            MisalignV_vsseg5e32_v_cg.sample(ins);
        end
        INSTR_VSSEG5E64_V     : begin
            MisalignV_vsseg5e64_v_cg.sample(ins);
        end
        INSTR_VSSEG5E8_V     : begin
            MisalignV_vsseg5e8_v_cg.sample(ins);
        end
        INSTR_VSSEG6E16_V     : begin
            MisalignV_vsseg6e16_v_cg.sample(ins);
        end
        INSTR_VSSEG6E32_V     : begin
            MisalignV_vsseg6e32_v_cg.sample(ins);
        end
        INSTR_VSSEG6E64_V     : begin
            MisalignV_vsseg6e64_v_cg.sample(ins);
        end
        INSTR_VSSEG6E8_V     : begin
            MisalignV_vsseg6e8_v_cg.sample(ins);
        end
        INSTR_VSSEG7E16_V     : begin
            MisalignV_vsseg7e16_v_cg.sample(ins);
        end
        INSTR_VSSEG7E32_V     : begin
            MisalignV_vsseg7e32_v_cg.sample(ins);
        end
        INSTR_VSSEG7E64_V     : begin
            MisalignV_vsseg7e64_v_cg.sample(ins);
        end
        INSTR_VSSEG7E8_V     : begin
            MisalignV_vsseg7e8_v_cg.sample(ins);
        end
        INSTR_VSSEG8E16_V     : begin
            MisalignV_vsseg8e16_v_cg.sample(ins);
        end
        INSTR_VSSEG8E32_V     : begin
            MisalignV_vsseg8e32_v_cg.sample(ins);
        end
        INSTR_VSSEG8E64_V     : begin
            MisalignV_vsseg8e64_v_cg.sample(ins);
        end
        INSTR_VSSEG8E8_V     : begin
            MisalignV_vsseg8e8_v_cg.sample(ins);
        end
        INSTR_VSSSEG2E16_V     : begin
            MisalignV_vssseg2e16_v_cg.sample(ins);
        end
        INSTR_VSSSEG2E32_V     : begin
            MisalignV_vssseg2e32_v_cg.sample(ins);
        end
        INSTR_VSSSEG2E64_V     : begin
            MisalignV_vssseg2e64_v_cg.sample(ins);
        end
        INSTR_VSSSEG2E8_V     : begin
            MisalignV_vssseg2e8_v_cg.sample(ins);
        end
        INSTR_VSSSEG3E16_V     : begin
            MisalignV_vssseg3e16_v_cg.sample(ins);
        end
        INSTR_VSSSEG3E32_V     : begin
            MisalignV_vssseg3e32_v_cg.sample(ins);
        end
        INSTR_VSSSEG3E64_V     : begin
            MisalignV_vssseg3e64_v_cg.sample(ins);
        end
        INSTR_VSSSEG3E8_V     : begin
            MisalignV_vssseg3e8_v_cg.sample(ins);
        end
        INSTR_VSSSEG4E16_V     : begin
            MisalignV_vssseg4e16_v_cg.sample(ins);
        end
        INSTR_VSSSEG4E32_V     : begin
            MisalignV_vssseg4e32_v_cg.sample(ins);
        end
        INSTR_VSSSEG4E64_V     : begin
            MisalignV_vssseg4e64_v_cg.sample(ins);
        end
        INSTR_VSSSEG4E8_V     : begin
            MisalignV_vssseg4e8_v_cg.sample(ins);
        end
        INSTR_VSSSEG5E16_V     : begin
            MisalignV_vssseg5e16_v_cg.sample(ins);
        end
        INSTR_VSSSEG5E32_V     : begin
            MisalignV_vssseg5e32_v_cg.sample(ins);
        end
        INSTR_VSSSEG5E64_V     : begin
            MisalignV_vssseg5e64_v_cg.sample(ins);
        end
        INSTR_VSSSEG5E8_V     : begin
            MisalignV_vssseg5e8_v_cg.sample(ins);
        end
        INSTR_VSSSEG6E16_V     : begin
            MisalignV_vssseg6e16_v_cg.sample(ins);
        end
        INSTR_VSSSEG6E32_V     : begin
            MisalignV_vssseg6e32_v_cg.sample(ins);
        end
        INSTR_VSSSEG6E64_V     : begin
            MisalignV_vssseg6e64_v_cg.sample(ins);
        end
        INSTR_VSSSEG6E8_V     : begin
            MisalignV_vssseg6e8_v_cg.sample(ins);
        end
        INSTR_VSSSEG7E16_V     : begin
            MisalignV_vssseg7e16_v_cg.sample(ins);
        end
        INSTR_VSSSEG7E32_V     : begin
            MisalignV_vssseg7e32_v_cg.sample(ins);
        end
        INSTR_VSSSEG7E64_V     : begin
            MisalignV_vssseg7e64_v_cg.sample(ins);
        end
        INSTR_VSSSEG7E8_V     : begin
            MisalignV_vssseg7e8_v_cg.sample(ins);
        end
        INSTR_VSSSEG8E16_V     : begin
            MisalignV_vssseg8e16_v_cg.sample(ins);
        end
        INSTR_VSSSEG8E32_V     : begin
            MisalignV_vssseg8e32_v_cg.sample(ins);
        end
        INSTR_VSSSEG8E64_V     : begin
            MisalignV_vssseg8e64_v_cg.sample(ins);
        end
        INSTR_VSSSEG8E8_V     : begin
            MisalignV_vssseg8e8_v_cg.sample(ins);
        end
        INSTR_VSUXEI16_V     : begin
            MisalignV_vsuxei16_v_cg.sample(ins);
        end
        INSTR_VSUXEI32_V     : begin
            MisalignV_vsuxei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSUXEI64_V     : begin
            MisalignV_vsuxei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSUXEI8_V     : begin
            MisalignV_vsuxei8_v_cg.sample(ins);
        end
        INSTR_VSUXSEG2EI16_V     : begin
            MisalignV_vsuxseg2ei16_v_cg.sample(ins);
        end
        INSTR_VSUXSEG2EI32_V     : begin
            MisalignV_vsuxseg2ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSUXSEG2EI64_V     : begin
            MisalignV_vsuxseg2ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSUXSEG2EI8_V     : begin
            MisalignV_vsuxseg2ei8_v_cg.sample(ins);
        end
        INSTR_VSUXSEG3EI16_V     : begin
            MisalignV_vsuxseg3ei16_v_cg.sample(ins);
        end
        INSTR_VSUXSEG3EI32_V     : begin
            MisalignV_vsuxseg3ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSUXSEG3EI64_V     : begin
            MisalignV_vsuxseg3ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSUXSEG3EI8_V     : begin
            MisalignV_vsuxseg3ei8_v_cg.sample(ins);
        end
        INSTR_VSUXSEG4EI16_V     : begin
            MisalignV_vsuxseg4ei16_v_cg.sample(ins);
        end
        INSTR_VSUXSEG4EI32_V     : begin
            MisalignV_vsuxseg4ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSUXSEG4EI64_V     : begin
            MisalignV_vsuxseg4ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSUXSEG4EI8_V     : begin
            MisalignV_vsuxseg4ei8_v_cg.sample(ins);
        end
        INSTR_VSUXSEG5EI16_V     : begin
            MisalignV_vsuxseg5ei16_v_cg.sample(ins);
        end
        INSTR_VSUXSEG5EI32_V     : begin
            MisalignV_vsuxseg5ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSUXSEG5EI64_V     : begin
            MisalignV_vsuxseg5ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSUXSEG5EI8_V     : begin
            MisalignV_vsuxseg5ei8_v_cg.sample(ins);
        end
        INSTR_VSUXSEG6EI16_V     : begin
            MisalignV_vsuxseg6ei16_v_cg.sample(ins);
        end
        INSTR_VSUXSEG6EI32_V     : begin
            MisalignV_vsuxseg6ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSUXSEG6EI64_V     : begin
            MisalignV_vsuxseg6ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSUXSEG6EI8_V     : begin
            MisalignV_vsuxseg6ei8_v_cg.sample(ins);
        end
        INSTR_VSUXSEG7EI16_V     : begin
            MisalignV_vsuxseg7ei16_v_cg.sample(ins);
        end
        INSTR_VSUXSEG7EI32_V     : begin
            MisalignV_vsuxseg7ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSUXSEG7EI64_V     : begin
            MisalignV_vsuxseg7ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSUXSEG7EI8_V     : begin
            MisalignV_vsuxseg7ei8_v_cg.sample(ins);
        end
        INSTR_VSUXSEG8EI16_V     : begin
            MisalignV_vsuxseg8ei16_v_cg.sample(ins);
        end
        INSTR_VSUXSEG8EI32_V     : begin
            MisalignV_vsuxseg8ei32_v_cg.sample(ins);
        end
`ifdef XLEN64
        INSTR_VSUXSEG8EI64_V     : begin
            MisalignV_vsuxseg8ei64_v_cg.sample(ins);
        end
`endif
        INSTR_VSUXSEG8EI8_V     : begin
            MisalignV_vsuxseg8ei8_v_cg.sample(ins);
        end
    endcase
endfunction
